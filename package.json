{
  "name": "clementine",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "chimp-watch": "chimp --ddp=http://localhost:3000 --watch --mocha --path=tests --browser=firefox",
    "chimp-test": "chimp --mocha --path=tests --browser=chrome"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "meteor-node-stubs": "~0.2.0"
  }
}
